sudo: required
services:
  - docker

before_install:
  - docker build -t asolerp/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true asolerp/react-test npm test

after_success:
  - docker build -t asolerp/docker-app ./client
